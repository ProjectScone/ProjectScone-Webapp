// Browser-test entry: compile the real production modules through Vite.
export {startVoiceDevice} from '../../Webapp/src/conversations/voice/device';
export {createApiClient} from '../../Webapp/src/api';
